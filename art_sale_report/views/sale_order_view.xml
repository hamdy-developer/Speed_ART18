<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_form_inherit_art_warehouse" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.art.warehouse</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <!-- Move warehouse_id to the header -->
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="partner_tags" widget="many2many_tags"/>
                <field name="picking_states"/>
                <field name="products_in_warehouse" invisible="1"/>
                <xpath expr="//form[1]/sheet[1]/notebook[1]/page[@name='other_information']/group[1]/group[@name='sale_shipping']/field[@name='warehouse_id'][2]"
                       position="move"/>
                       
            </xpath>
           <!-- <xpath expr="//field[@name='order_line']/list/field[@name='product_template_id']" position="attributes">--> 
               <!-- <attribute name="domain">[('id', 'in', parent.products_in_warehouse), ('sale_ok', '=', True), '|', ('company_id', '=', False), ('company_id', '=', parent.company_id)]</attribute> -->
        <!-- </xpath>  -->
           
        </field>
    </record>

    <record id="view_order_tree_inherit_art_warehouse" model="ir.ui.view">
        <field name="name">sale.order.tree.inherit.art.warehouse</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.sale_order_tree"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="partner_tags" widget="many2many_tags" optional="show"/>
                <field name="picking_states" optional="show"/>
            </field>
        </field>
    </record>

    <record id="view_sales_order_filter_inherit_art_warehouse" model="ir.ui.view">
        <field name="name">sale.order.search.inherit.art.warehouse</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_sales_order_filter"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="partner_tags" filter_domain="[('partner_tags', 'ilike', self)]"/>
            </field>
        </field>
    </record>
</odoo>
